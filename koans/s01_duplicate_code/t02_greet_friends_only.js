const code = {
    greet(name) {
        if (name === 'Amy') {
            return 'Hello Amy!';
        }

        if (name === 'Bob') {
            return 'Hello Bob!';
        }

        return '';
    },
};

module.exports = code;